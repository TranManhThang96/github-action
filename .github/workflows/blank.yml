name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello World"
          echo "Hello World2"
      - name: Print goodbye
        run: echo "Done - bye!"
      - name: Install Nodejs
        uses: actions/setup-node@v4
        with:
          node-version: 18
